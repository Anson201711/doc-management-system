# 📄 文档模块测试用例

**版本**: V1.0  
**日期**: 2026-03-18  
**测试模块**: 文档模块 (QA-03)

---

## 用例总览

| 用例ID | 用例名称 | 优先级 | 前置条件 |
|--------|----------|--------|----------|
| DC-01 | 创建文档 | P0 | 登录成功 |
| DC-02 | 查询文档列表 | P0 | 存在文档 |
| DC-03 | 查询文档详情 | P0 | 文档已创建 |
| DC-04 | 更新文档 | P0 | 文档已创建 |
| DC-05 | 删除文档 | P0 | 文档已创建 |
| DC-06 | 文档搜索 | P0 | 存在文档 |
| DC-07 | 版本历史 | P1 | 文档有多版本 |
| DC-08 | 版本回滚 | P1 | 文档有多版本 |

---

## 详细测试用例

### DC-01: 创建文档

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-01 |
| **用例名称** | 创建文档 |
| **测试目的** | 验证用户可以成功创建新文档 |
| **优先级** | P0 |
| **前置条件** | 用户已登录 |
| **测试步骤** | 1. 调用创建文档接口: `POST /api/documents`<br>2. 传入文档标题: "测试文档"<br>3. 传入文档内容: "这是测试内容"<br>4. 传入文档类型: "docx"<br>5. 传入文件夹ID (可选) |
| **预期结果** | 1. 创建成功<br>2. 返回文档ID<br>3. 文档信息包含: ID、标题、内容、创建时间、创建者<br>4. 响应状态码 201 |
| **后置条件** | 新文档已创建，可查询 |

---

### DC-02: 查询文档列表

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-02 |
| **用例名称** | 查询文档列表 |
| **测试目的** | 验证用户可以查询自己的文档列表 |
| **优先级** | P0 |
| **前置条件** | 系统中存在已创建的文档 |
| **测试步骤** | 1. 用户登录<br>2. 调用查询文档列表接口: `GET /api/documents`<br>3. 可选: 传入分页参数 (page=1, size=10)<br>4. 可选: 传入排序参数 (sort=createdAt,desc) |
| **预期结果** | 1. 查询成功<br>2. 返回文档列表<br>3. 每个文档包含: ID、标题、创建时间、更新时间、创建者<br>4. 支持分页返回<br>5. 响应状态码 200 |
| **后置条件** | 无 |

---

### DC-03: 查询文档详情

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-03 |
| **用例名称** | 查询文档详情 |
| **测试目的** | 验证用户可以查看文档的完整内容 |
| **优先级** | P0 |
| **前置条件** | 文档已创建 |
| **测试步骤** | 1. 用户登录<br>2. 获取已存在的文档ID<br>3. 调用查询文档详情接口: `GET /api/documents/{id}` |
| **预期结果** | 1. 查询成功<br>2. 返回完整文档信息: ID、标题、内容、类型、创建时间、更新时间、创建者、版本号<br>3. 响应状态码 200 |
| **后置条件** | 无 |

---

### DC-04: 更新文档

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-04 |
| **用例名称** | 更新文档 |
| **测试目的** | 验证用户可以成功更新文档内容 |
| **优先级** | P0 |
| **前置条件** | 文档已创建 |
| **测试步骤** | 1. 用户登录<br>2. 获取已存在的文档ID<br>3. 调用更新文档接口: `PUT /api/documents/{id}`<br>4. 传入更新后的标题: "更新后的标题"<br>5. 传入更新后的内容: "更新后的内容" |
| **预期结果** | 1. 更新成功<br>2. 返回更新后的文档信息<br>3. 版本号自动递增<br>4. 更新时间已刷新<br>5. 响应状态码 200 |
| **后置条件** | 文档已更新，版本+1 |

---

### DC-05: 删除文档

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-05 |
| **用例名称** | 删除文档 |
| **测试目的** | 验证用户可以成功删除文档 |
| **优先级** | P0 |
| **前置条件** | 文档已创建 |
| **测试步骤** | 1. 用户登录<br>2. 获取已存在的文档ID<br>3. 调用删除文档接口: `DELETE /api/documents/{id}` |
| **预期结果** | 1. 删除成功<br>2. 返回成功提示<br>3. 文档从列表中移除<br>4. 响应状态码 204 |
| **后置条件** | 文档已删除 (软删除/回收站) |

---

### DC-06: 文档搜索

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-06 |
| **用例名称** | 文档搜索 |
| **测试目的** | 验证用户可以通过关键词搜索文档 |
| **优先级** | P0 |
| **前置条件** | 系统中存在包含特定关键词的文档 |
| **测试步骤** | 1. 用户登录<br>2. 调用搜索接口: `GET /api/documents/search`<br>3. 传入搜索关键词: `keyword`<br>4. 可选: 传入搜索范围 (title, content, all) |
| **预期结果** | 1. 搜索成功<br>2. 返回匹配的文档列表<br>3. 匹配项高亮显示<br>4. 支持分页<br>5. 响应状态码 200 |
| **后置条件** | 无 |

---

### DC-07: 版本历史

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-07 |
| **用例名称** | 版本历史 |
| **测试目的** | 验证用户可以查看文档的版本历史记录 |
| **优先级** | P1 |
| **前置条件** | 文档有多个版本 (至少更新2次) |
| **测试步骤** | 1. 用户登录<br>2. 获取已存在的文档ID<br>3. 调用版本历史接口: `GET /api/documents/{id}/versions` |
| **预期结果** | 1. 查询成功<br>2. 返回版本列表<br>3. 每个版本包含: 版本号、修改时间、修改人、变更说明<br>4. 响应状态码 200 |
| **后置条件** | 无 |

---

### DC-08: 版本回滚

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-08 |
| **用例名称** | 版本回滚 |
| **测试目的** | 验证用户可以将文档回滚到指定版本 |
| **优先级** | P1 |
| **前置条件** | 文档有多个版本 |
| **测试步骤** | 1. 用户登录<br>2. 获取已存在的文档ID<br>3. 获取版本历史，选择目标版本号<br>4. 调用版本回滚接口: `POST /api/documents/{id}/versions/{version}/rollback` |
| **预期结果** | 1. 回滚成功<br>2. 文档内容恢复到指定版本<br>3. 新增一个回滚后的版本记录<br>4. 响应状态码 200 |
| **后置条件** | 文档已回滚，新版本已创建 |

---

## 测试数据

### 测试文档

| 文档ID | 标题 | 类型 | 创建者 |
|--------|------|------|--------|
| DOC-001 | 测试文档1 | docx | admin |
| DOC-002 | 项目计划书 | docx | dept_manager |
| DOC-003 | 技术方案 | docx | creator |

---

## API接口清单

| 接口路径 | 方法 | 说明 |
|----------|------|------|
| `/api/documents` | GET | 查询文档列表 |
| `/api/documents` | POST | 创建文档 |
| `/api/documents/{id}` | GET | 查询文档详情 |
| `/api/documents/{id}` | PUT | 更新文档 |
| `/api/documents/{id}` | DELETE | 删除文档 |
| `/api/documents/search` | GET | 文档搜索 |
| `/api/documents/{id}/versions` | GET | 版本历史 |
| `/api/documents/{id}/versions/{version}/rollback` | POST | 版本回滚 |

---

## 评论与批注测试用例

### DC-09: 添加评论

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-09 |
| **用例名称** | 添加评论 |
| **测试目的** | 验证用户可以成功添加评论 |
| **优先级** | P0 |
| **前置条件** | 用户已登录，文档已创建 |
| **测试步骤** | 1. 调用添加评论接口: `POST /api/v1/comments`<br>2. 传入文档ID<br>3. 传入评论内容: "这是评论内容" |
| **预期结果** | 1. 创建成功<br>2. 返回评论ID<br>3. 响应状态码 201 |

---

### DC-10: 查询评论列表

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-10 |
| **用例名称** | 查询评论列表 |
| **测试目的** | 验证用户可以查看文档的评论列表 |
| **优先级** | P0 |
| **前置条件** | 文档已有评论 |
| **测试步骤** | 1. 调用查询评论列表: `GET /api/v1/comments?docId=xxx` |
| **预期结果** | 1. 返回评论列表<br>2. 每个评论包含: ID、内容、创建者、时间 |

---

### DC-11: 回复评论

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-11 |
| **用例名称** | 回复评论 |
| **测试目的** | 验证用户可以回复已有评论 |
| **优先级** | P1 |
| **前置条件** | 已有评论存在 |
| **测试步骤** | 1. 调用回复接口: `POST /api/v1/comments/{id}/reply`<br>2. 传入回复内容 |
| **预期结果** | 1. 创建成功<br>2. 评论关联到父评论 |

---

### DC-12: 添加批注

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-12 |
| **用例名称** | 添加批注 |
| **测试目的** | 验证用户可以在文档特定位置添加批注 |
| **优先级** | P0 |
| **前置条件** | 用户已登录，文档已创建 |
| **测试步骤** | 1. 调用添加批注接口: `POST /api/v1/annotations`<br>2. 传入文档ID<br>3. 传入位置坐标和内容 |
| **预期结果** | 1. 创建成功<br>2. 返回批注ID和位置信息 |

---

### DC-13: 查询批注列表

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-13 |
| **用例名称** | 查询批注列表 |
| **测试目的** | 验证用户可以查看文档的批注列表 |
| **优先级** | P0 |
| **前置条件** | 文档已有批注 |
| **测试步骤** | 1. 调用查询批注列表: `GET /api/v1/annotations?docId=xxx` |
| **预期结果** | 1. 返回批注列表<br>2. 每个批注包含: ID、位置、内容、创建者 |

---

### DC-14: 删除批注

| 字段 | 内容 |
|------|------|
| **用例ID** | DC-14 |
| **用例名称** | 删除批注 |
| **测试目的** | 验证用户可以删除自己的批注 |
| **优先级** | P1 |
| **前置条件** | 批注已创建 |
| **测试步骤** | 1. 调用删除批注: `DELETE /api/v1/annotations/{id}` |
| **预期结果** | 1. 删除成功<br>2. 响应状态码 204 |

---

## 负向用例补充

| 用例ID | 用例名称 | 测试场景 | 预期结果 |
|--------|----------|----------|----------|
| DC-02-N1 | 查询空列表 | 用户无任何文档 | 返回空列表，状态码200 |
| DC-03-N1 | 查询不存在文档 | 文档ID不存在 | 返回404，提示文档不存在 |
| DC-04-N1 | 更新不存在文档 | 文档ID不存在 | 返回404，提示文档不存在 |
| DC-05-N1 | 删除不存在文档 | 文档ID不存在 | 返回404，提示文档不存在 |
| DC-06-N1 | 搜索无结果 | 关键词不匹配 | 返回空列表，状态码200 |

---

*创建时间: 2026-03-18 15:14*  
*更新时间: 2026-03-18 15:19*